{% set name = "gdbm" %}
{% set version = "1.18" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: ftp://ftp.gnu.org/gnu/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha1: 0f1056ab3beb1bf09dad06f8083f2c79716a5b1b
  patches:
    # From https://github.com/spack/spack/commit/6d7daf0def6b8c432b5e79d48c96f29faf3d2db9
    - gdbm_gcc_10.patch
    - osx_arm64.patch   # [osx and arm64]

build:
  number: 4
  missing_dso_whitelist:  # [linux and s390x]
    - $RPATH/ld64.so.1    # [linux and s390x]
  run_exports:
    # Please see ABI report
    # https://abi-laboratory.pro/index.php?view=timeline&l=gdbm
    - {{ pin_subpackage(name, max_pin='x.x') }}
  skip: True  # [win]

requirements:
  build:
    - libtool   # [unix]
    - {{ compiler('c') }}
    - make
    - patch     # [not win]
    - m2-patch  # [win]
  host:
    - readline

test:
  commands:
    - gdbm_dump --version
    - gdbm_load --version
    - gdbmtool --version

about:
  home: https://www.gnu.org.ua/software/gdbm/
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: COPYING
  summary: >
    A library of database functions that use extensible hashing and works
    similar to the standard UNIX dbm functions.
  doc_url: https://www.gnu.org.ua/software/gdbm/
  dev_url: https://www.gnu.org.ua/software/gdbm/

extra:
  recipe-maintainers:
    - hmaarrfk
    - jakirkham
